# Public Assets

Static assets belong here. Do not commit production secrets or private vendor media.

Vendor-uploaded images do not belong in this directory. Runtime vendor photos are written to Supabase Storage through the admin Vendor Images uploader and rendered from `vendor_images` metadata rows.
