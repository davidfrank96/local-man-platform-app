# Types

Shared TypeScript types belong here.

- `domain.ts` contains inferred domain and request/response data types.
- `api.ts` exposes the standard API response and endpoint contract types.
- `index.ts` re-exports public shared types.

Runtime validation schemas live in `lib/validation/`.
